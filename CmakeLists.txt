cmake_minimum_required(VERSION 3.10)

project(Autentikointijarjestelma)

set(CMAKE_CXX_STANDARD 11)

# Lisätään hakemisto, jossa lähdekoodit sijaitsevat
include_directories(${PROJECT_SOURCE_DIR})

# Lisää lähdekoodit ja luo kohde
add_executable(AuthenticationSystem auth.c test_auth.c)

# Lisää testauskehykset
target_link_libraries(AuthenticationSystem gtest gtest_main)
